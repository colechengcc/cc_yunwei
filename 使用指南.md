# Docker 代理问题解决方案 - 不修改 Dockerfile

## 🎯 你的问题

- **错误**：apt 使用了错误的代理 `172.16.21.69:7897`（502 Bad Gateway）
- **期望**：使用正确的代理 `172.16.21.5:7890`
- **要求**：**不修改 Dockerfile**

---

## ⚡ 三秒钟解决方案

```bash
# 方案A：一次性构建（推荐先试这个）
docker build \
  --build-arg HTTP_PROXY=http://172.16.21.5:7890 \
  --build-arg HTTPS_PROXY=http://172.16.21.5:7890 \
  --build-arg http_proxy=http://172.16.21.5:7890 \
  --build-arg https_proxy=http://172.16.21.5:7890 \
  --no-cache \
  -f Dockerfile.1.91-cross-aarch64-linux-gnu \
  -t rust-cross:1.91 \
  .
```

```bash
# 方案B：使用提供的脚本（更简单）
./build-no-dockerfile-change.sh Dockerfile.1.91-cross-aarch64-linux-gnu rust-cross:1.91
```

---

## 📋 完整步骤（推荐）

### 第一步：测试环境

```bash
chmod +x test-proxy.sh
./test-proxy.sh
```

这个脚本会：
- ✅ 测试 172.16.21.5:7890 是否可用
- ❌ 测试 172.16.21.69:7897 是否可用
- 🔍 检查所有 Docker 代理配置
- 🧪 运行测试构建

### 第二步：根据测试结果选择方案

#### 如果当前代理可用 ✅

**直接构建：**

```bash
chmod +x build-no-dockerfile-change.sh
./build-no-dockerfile-change.sh Dockerfile.1.91-cross-aarch64-linux-gnu rust-cross:1.91
```

#### 如果想要永久配置 🔧

```bash
chmod +x setup-docker-proxy.sh
./setup-docker-proxy.sh
```

配置完成后，以后所有构建都自动使用正确的代理：

```bash
# 无需任何额外参数
docker build -f Dockerfile.1.91-cross-aarch64-linux-gnu -t rust-cross:1.91 .
```

---

## 🛠️ 三个核心工具

| 脚本 | 作用 | 使用场景 |
|------|------|---------|
| `test-proxy.sh` | 诊断所有代理配置 | 首次使用、故障排查 |
| `build-no-dockerfile-change.sh` | 带代理参数构建 | 每次构建（临时方案） |
| `setup-docker-proxy.sh` | 永久配置 BuildKit | 一次配置，永久生效 |

---

## 📖 详细方案对比

### 方案 1：使用 --build-arg（临时）

**优点：**
- ✅ 不修改任何文件
- ✅ 立即生效
- ✅ 灵活，可以为不同构建指定不同代理

**缺点：**
- ❌ 每次构建都要加参数
- ❌ 命令行比较长

**使用方法：**
```bash
docker build \
  --build-arg HTTP_PROXY=http://172.16.21.5:7890 \
  --build-arg HTTPS_PROXY=http://172.16.21.5:7890 \
  --build-arg http_proxy=http://172.16.21.5:7890 \
  --build-arg https_proxy=http://172.16.21.5:7890 \
  --no-cache \
  -f Dockerfile.1.91-cross-aarch64-linux-gnu \
  -t rust-cross:1.91 \
  .
```

### 方案 2：配置 ~/.docker/config.json（永久）

**优点：**
- ✅ 配置一次，永久生效
- ✅ 不修改 Dockerfile
- ✅ 所有构建自动使用

**缺点：**
- ❌ 需要重启 Docker
- ❌ 影响所有构建（如果需要不同代理可能不适合）

**使用方法：**
```bash
./setup-docker-proxy.sh
```

或手动创建：
```bash
mkdir -p ~/.docker
cat > ~/.docker/config.json << 'EOF'
{
  "proxies": {
    "default": {
      "httpProxy": "http://172.16.21.5:7890",
      "httpsProxy": "http://172.16.21.5:7890",
      "noProxy": "127.0.0.1,localhost,reg.smvm.cn"
    }
  }
}
EOF

sudo systemctl restart docker
```

### 方案 3：使用 docker buildx

**优点：**
- ✅ 更现代的构建方式
- ✅ 支持多平台构建
- ✅ 代理配置隔离

**缺点：**
- ❌ 配置稍复杂
- ❌ 需要了解 buildx

**使用方法：**
```bash
# 创建带代理的 builder
docker buildx create --name mybuilder \
  --driver docker-container \
  --driver-opt env.HTTP_PROXY=http://172.16.21.5:7890 \
  --driver-opt env.HTTPS_PROXY=http://172.16.21.5:7890 \
  --use

# 使用该 builder 构建
docker buildx build \
  -f Dockerfile.1.91-cross-aarch64-linux-gnu \
  -t rust-cross:1.91 \
  --load \
  .
```

---

## 🔍 故障排查

### 问题 1：仍然报 502 Bad Gateway

**原因：** 使用了旧的构建缓存

**解决：**
```bash
# 清除所有构建缓存
docker builder prune -af

# 使用 --no-cache 重新构建
./build-no-dockerfile-change.sh Dockerfile.1.91-cross-aarch64-linux-gnu rust-cross:1.91
```

### 问题 2：代理连接超时

**检查代理是否正常：**
```bash
curl -x http://172.16.21.5:7890 http://archive.ubuntu.com
```

**如果失败：**
- 检查代理服务器是否运行
- 检查防火墙规则
- 确认代理地址和端口正确

### 问题 3：环境变量不生效

**原因：** BuildKit 可能没有正确读取环境变量

**解决：**
```bash
# 确保 BuildKit 已启用
cat /etc/docker/daemon.json

# 应该包含：
# "features": { "buildkit": true }

# 如果没有，添加后重启
sudo systemctl restart docker
```

---

## 💡 为什么会出现 172.16.21.69？

### 根本原因

Docker 守护进程的代理配置**只对 Docker 本身有效**（拉取镜像时），**不会传递给构建过程中的容器**。

### 可能的来源

1. **基础镜像的 APT 配置**
   ```bash
   # 检查基础镜像
   docker run --rm ubuntu:20.04 cat /etc/apt/apt.conf.d/* 2>/dev/null
   ```

2. **构建缓存层**
   - 旧的构建层可能包含旧的代理配置
   - 解决：使用 `--no-cache`

3. **环境变量继承**
   - 你的 /etc/profile 中有注释掉的旧配置
   - 可能在某些情况下被读取

---

## 📚 文档导航

- **快速开始** → `README_NO_DOCKERFILE_CHANGE.md`
- **所有解决方案** → `NO_DOCKERFILE_CHANGE_SOLUTION.md`
- **问题详细分析** → `PROXY_ISSUE_ANALYSIS.md`
- **快速答案** → `QUICK_ANSWER.md`

---

## ✅ 推荐流程（最佳实践）

```bash
# 1️⃣ 首次使用：测试环境
./test-proxy.sh

# 2️⃣ 如果是长期使用：永久配置
./setup-docker-proxy.sh

# 3️⃣ 之后的构建（配置后）
docker build -f Dockerfile.1.91-cross-aarch64-linux-gnu -t rust-cross:1.91 .

# 或者每次构建时（不配置）
./build-no-dockerfile-change.sh Dockerfile.1.91-cross-aarch64-linux-gnu rust-cross:1.91
```

---

## 🎁 总结

| 需求 | 解决方案 | 命令 |
|------|---------|------|
| 快速测试 | build-no-dockerfile-change.sh | `./build-no-dockerfile-change.sh` |
| 长期使用 | setup-docker-proxy.sh | `./setup-docker-proxy.sh` |
| 故障诊断 | test-proxy.sh | `./test-proxy.sh` |
| 手动构建 | --build-arg | 见方案1命令 |

**最简单的开始：**
```bash
./test-proxy.sh                    # 看看问题在哪
./setup-docker-proxy.sh            # 一次配置
docker build -f your.dockerfile .  # 正常构建
```

---

所有方案都**不需要修改 Dockerfile**！🎉
